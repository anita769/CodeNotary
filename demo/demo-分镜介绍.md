# demo-v9.mp4 分镜介绍（复赛演示·新前端版）

> 与 demo-v8 的关系：**叙事、字幕、数字口径与 v8 完全一致**；画面中的旧深色 Console/办事大厅全部替换为现役浅色前端（任务看板/办事大厅/任务页/Skill 看板），数据仍是同批真实 run（`mb_delivery_semantics` 等 8.29 live 证据）。AgentTeams 聊天室段落原样保留。新增一段 GitHub 接入演示（取自真实 PR #2 录像）。

- **视频时长**：约 5 分 48 秒（1920×1080）
- **演示主题**：CodeNotary × AgentTeams —— AI 多智能体团队修复代码缺陷的全流程「公证」演示

---

## 第一幕：多智能体团队在聊天室中修复缺陷（0:00 – 1:01）

**画面与 v8 完全相同（AgentTeams 聊天室原录）**。admin 以自然语言下达任务（场景 `mb_delivery_semantics`，ISSUE-301），十棒接力：哨兵→分诊→根因→契约→作者/盲测（双轨互盲）→门禁（M05 被精确边界断言击杀，M03/M06 举证为等价变异体）→发布（25/25）→复盘（沉淀 Skill、60 份证据哈希封存）。

## 第二幕：任务页——三门禁与公证书（1:01 – 1:42）

| 时间点 | 画面（新前端） | 字幕 |
|---|---|---|
| 1:01 | `/run` 任务页（`mb_delivery_semantics`）：横屏 stepper 十棒全绿、14 状态机 RELEASED、指标网格（87 次网关调用、0 人工介入）、处理过程表 | 三道审核全部通过：功能测试 25/25、变异测试得分 1.00、规范检查零违规 |
| 1:15 | 「验证结果」tab：规范检查/变异测试/测试门禁三行全绿 | 同上 |
| 1:24 | 办事大厅：公证书信封（发生了什么/意味着什么/您能做什么/证据在哪/下一步 五要素）+ 三门禁进展卡 | 公证书签发——证据包可下载，任何人都能用 make verify 自行复算 |
| 1:33 | 「审计记录」tab：trace 表逐行留痕，`notary_skill.match`（author 角色、CONTRACTED 阶段）在画面中 | 真实运行的 Skill 调用记录：哪个角色、在哪个阶段、调用了哪个 Skill，输入输出均计算哈希存档 |

## 第三幕：Skill 看板——活的经验台账（1:42 – 1:58）

| 时间点 | 画面 | 字幕 |
|---|---|---|
| 1:42 | Skill 看板：12 张 Skill 卡（含 `delivery-semantics-review`，标注 distilled from run mb_delivery_semantics）、信号与适用角色 | Skill 不是静态文档——运行中按条件匹配加载，每次注册都记入带哈希的台账 |
| 1:52 | 追认队列：试用徽章 + 追认转正/一票否决按钮 | 新沉淀的 Skill 先进试用区，由人投票追认或一票否决 |

## 第四幕：人工裁决——系统拿不准就停下来（1:58 – 3:54）

本幕 HITL 两段（修订/否决）是在同一工单的两次真实运行上实操录制的：工件（根因诊断、盲测 21 用例、契约措辞、反驳论证）全部取自 8.29 真实 run，裁决为控制台真签（签署人 陈工，JWT 留痕）。

| 时间点 | 画面 | 字幕 |
|---|---|---|
| 1:58 | `/run`（`mb_delivery_hitl_ruling`）：stepper 门禁未通过（红）+ 人工裁决待裁决（紫），处理过程含「争议·修复方异议」行（dead_letters 元素格式两种解读） | 发现需求描述存在两种合理解读，流水线暂停运行，等待人工裁决 |
| 2:07 | 工作台裁决卡：双方立场（规则 v1 六条）、修复方异议全文、客观证据（26 用例存在未通过项）、四选一裁决 | 这类决策交由人工裁定，系统不代为处置 |
| 2:18 | **真签「修订契约」全程**：修订文本框改断言、v1.0↔v1.1 对照、勾选引用证据、填理由、令牌签署、二次确认回显 | 裁决签署：契约升 v1.1，代码按新规则重验——签署人、通道、引用材料全部进证据包 |
| 2:40 | （v8 原录）聊天室：系统「等待人工裁决」请示 → admin 回复裁决 → 流水线继续 | 人工裁决后，流水线按裁决继续执行直至发布 |
| 2:54 | 「验收契约」tab：v1.1 六条断言（格式已按裁决钉死） | 裁决后的措辞进入契约 v1.1，v1.0 封存可查、永不覆盖 |
| 3:04 | `/run` 终态：三门禁全绿 RELEASED、处理过程含「人工裁决 #1 修订契约 · 签署：陈工（console-token）」 | 流水线按裁决继续执行，三道门禁全绿直至发布 |
| 3:17 | 反向演示（`mb_delivery_hitl_veto`）：裁决卡四选一 | 人工同样可以否决——流水线立即终止 |
| 3:26 | **真签「维持契约」** → REJECTED，证据照常封印（10 文件） | 人工同样可以否决——流水线立即终止，已产生的证据照常封存 |
| 3:43 | `/run`（veto 终态）：审计留痕「人工裁决 #1 维持契约 · 签署：陈工」 | 否决同样签名留痕：裁决人、理由、时间全部可查 |

## 第五幕：对抗性场景——外部送审拦截与行为否决（3:54 – 4:36）

| 时间点 | 画面 | 字幕 |
|---|---|---|
| 3:54 | 任务看板「未放行」列：`mb_external_breaking/dupe/reorder` 三张红卡 | 三次外部 AI 送审均被拦下 |
| 4:03 | `/run`（`qb_external_leak`）：分诊「不受理」，验收未通过 | 修复夹带未申报的「历史预加载」功能——文件打开后未释放 |
| 4:10 | `/run`（`mb_external_breaking`）：dispatch_all 返回值 int→dict，REJECTED | 擅自修改公开接口的返回类型 |
| 4:17 | `/run`（`zz_l1_replay_leak`）：测试门禁绿、变异门禁绿、**规范检查红（1 否决级）** | 测试全绿，行为审查仍可否决（本卡为裁决层确定性回放） |
| 4:26 | 「审计记录」tab：调用轨迹表（时间·角色·工具·状态·哈希链），角色各司其职 | 每个角色只能调用自己权限内的接口，全程零越权 |

## 第六幕：证据可验与交付（4:36 – 5:04）

| 时间点 | 画面 | 字幕 |
|---|---|---|
| 4:36 | 办事大厅全貌：左列任务列表（同一工单多版标注），右侧证书信封时间线 | 一次公证的所有事实——状态流转、人工裁决、门禁结果、证据文件——归档在同一编号下 |
| 4:46 | 「证据」tab：证据文件按 SHA-256 封存，逐项可展开原始数据 | 任何一份证据文件都可以独立重算哈希，与封存记录逐位核对 |
| 4:56 | 交付物卡：修复文件 + 公证书下载，逐文件带指纹 | 修复成品与公证书可直接下载，逐文件带指纹 |

## 第七幕：接入 GitHub（5:04 – 5:34）

画面为真实录像：演示仓库 `anita769/codenotary-demo` PR #2（已真实合并）。

| 时间点 | 画面 | 字幕 |
|---|---|---|
| 5:04 | GitHub PR 页：红叉 check + 公证处 A1 叙事评论（失败用例、根因、三选择指引） | 同一套公证流水线可以直接接入 GitHub：PR 送审即受理，审核意见写回 PR 评论 |
| 5:16 | Actions 运行详情：jobs 全绿 + EvidencePack artifact | 修复后再审，三道门禁全部转绿，证据包随构建归档 |
| 5:26 | PR merged 紫标 | 验收通过之后，合并动作只属于人、只在系统之外完成 |

## 收尾（5:34 – 5:48）

黑卡两张：
1. 「回放 35/35 逐位一致 · 47 项异常输入全部安全拦截 · 进程意外中断后可从断点恢复执行」
2. 「六条真实流水线，全程留痕 —— CodeNotary × AgentTeams」+ **开源地址：github.com/anita769/CodeNotary（Apache-2.0）**

---

## 制作注记（诚实边界）

- v8 → v9 的替换仅限**前端皮肤**：所有展示数据来自同批真实 run（8.29 live 证据包，哈希可复算），字幕数字口径未改。
- 第四幕两条 HITL run（`_ruling`/`_veto`）是按同一工单场景、用真实工件在新网关上真实重跑+真签得到的（2026-09-21 录制），裁决留痕 `adjudication.json` 可查；原 8.29 首轮 REJECTED/dispute 的 run 数据未留存，v8 该镜亦为重放。
- `zz_l1_replay_leak` 为裁决层确定性回放 run（沿用 v8 的 REPLAY 角标制度，字幕内标注）。
- 旧 Console 的「场景画廊/权限审计矩阵」两页在新前端无对应页，按叙事映射为任务看板与「审计记录」tab；场景画廊镜已删去。
- 第七幕 GitHub 素材为 2026-09-18 真实录像（PR #2 已合并，证据链锚在 GitHub 历史上）。
